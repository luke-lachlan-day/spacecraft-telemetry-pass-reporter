# Spacecraft Telemetry Pass Reporter 1.1.2

## What the application does

This AI-assisted learning and portfolio project provides a local Windows desktop application and a
typed Python command-line interface. Both validate fictional spacecraft-pass telemetry and produce
polished, self-contained HTML reports through the same Python schema, analyser, and renderer.

## Desktop reliability

- Cancelling or failing a JSON import now preserves the current editor, analysis result, preview,
  and save actions.
- Import errors are reported without marking unchanged editor fields as invalid.
- Starting a new analysis immediately retires the previous visible result until fresh output is
  ready.

## Release quality

Windows asset publication is now gated on the complete reusable Quality workflow: formatting,
linting, strict typing, the Python 3.11-3.14 test matrix, clean-wheel package smoke testing, and
Playwright browser checks. The Windows build then runs its packaged self-test and real WebView2 UI
smoke test before producing immutable versioned assets.

## Download and verification

Download the versioned Windows x64 ZIP, matching `.sha256`, and build-information file from this
release. Verify the ZIP checksum, extract the complete folder, and run `Telemetry Reporter.exe`
beside its `_internal` folder. Python is not required.

The portable application is unsigned, so Windows SmartScreen may show an unrecognized-app warning.
It requires Microsoft Edge WebView2 Runtime, which current Windows 10 and Windows 11 installations
normally include.

**Full Changelog:** https://github.com/luke-lachlan-day/spacecraft-telemetry-pass-reporter/compare/v1.1.1...v1.1.2
